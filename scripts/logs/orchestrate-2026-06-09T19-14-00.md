# Orchestration Log — 2026-06-09T19-14-00

**Mode:** Broad audit

---

## Plan (3 tasks)

1. **Add notification preferences UI** (medium, feature)
   Create a new component `NotificationPreferences.tsx` in `app/profile/` to allow users to opt-in/out of different email types. Use checkboxes for each notification type and save preferences to the database. Update the `ProfilePage` to include this component.

2. **Verify Resend sender domain** (small, production)
   Verify a sender domain in the Resend dashboard (e.g. `camp@glaum.camp`). Update the `FROM` constant in `lib/send-email.ts` once verified.

3. **Swap Clerk dev keys for prod keys** (small, production)
   Update `.env.local` with production Clerk keys. Replace `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY` with production keys. Update Vercel environment variables accordingly.

---

