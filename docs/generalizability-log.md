# Generalizability Log

A **running ledger of everything Glåüm/What If–specific or single-community–assuming** in the codebase. This is the requirements source for the multi-tenant foundation we'll build *after* What If (July 23, 2026). The roadmap & resolution plan lives in [`multi-community.md`](./multi-community.md) — this file is the raw, append-as-you-go ledger that feeds it.

> **Why this exists:** What If is the dogfood. The single best way to learn what's truly core vs. Glåüm-specific is to build/run the real event and notice every place the app assumes "Glåüm" or "What If." Captured from reality, not guessed.

## Maintenance protocol — for Claude (automatic, every iteration)

During **any** work on this codebase, whenever you write or read code that:
- hardcodes a Glåüm- or What-If–specific value (name, term, color, copy, option list, image, font, domain), **or**
- assumes there is only one community (global query, global unique constraint, global storage path, single auth scope), **or**
- makes something awkward to vary per-community,

→ **add a row to the table below before you finish the task.** Rules:
- **Log it, don't necessarily fix it.** During What If we ship single-tenant; the point is to *capture*, not detour. Only fix in-place if it's trivial and already in your task's scope (e.g. you're editing that exact line and `SITE_NAME` is a one-word swap).
- **Prefer config-first when you DO touch it.** If implementing a Glåüm-specific need for What If, route it through `page_content`/`site-config` rather than a hardcode, even when slightly more work — that way the dogfood builds the customization layer instead of debt.
- Keep entries terse. One row. Link `file:line` where useful.
- Mark **Status**: `open` (needs work in the foundation phase) · `good-pattern` (already done right, kept as a reference example) · `done`.

## Categories (for the "Type" column)
`copy` · `terminology` · `branding` (color/font/image) · `option-list` · `schema` (column/table) · `auth` · `storage` · `domain/routing` · `email` · `query-scope`

---

## Ledger

| Date | Area / `file` | Type | What's Glåüm/What-If–specific | Suggested config approach | Status |
|---|---|---|---|---|---|
| 2026-06-30 | `lib/site-config.ts`, `app/manifest.ts`, `app/layout.tsx` | branding/copy | Site/event name + PWA manifest read from env (`SITE_NAME`/`EVENT_NAME`) instead of hardcoded | Already env-driven; PWA manifest dynamic per deploy | good-pattern |
| 2026-06-30 | Inline hex colors across ~all components (`#1A0A24`/`#C8A848`/`#D239F8`/`#F3EDE6`) | branding | Brand palette hardcoded inline in every component (per design-system.md) — biggest theming blocker | Per-community `community_config` color keys → CSS custom properties injected at layout level | open |
| 2026-06-30 | `/api/badge`, `public/badge_base.png`, `TokyoDreams` font | branding | Badge base image + display font are local Glåüm assets | `community_config` `badge_base_url` + `badge_font_url`; load remotely | open |
| 2026-06-30 | Terminology: "Attunement", "Many Hands", form step subtitles | terminology | Glåüm ceremonial vocabulary baked into UI | `community_config` term keys (`term_onboarding`, etc.) | open |
| 2026-06-30 | `ApplyWizard.tsx` "Shrimp" step / `applications.shrimp_relationship` | schema | Culture-specific question as a real DB column | Move to `custom_answers JSONB` in a future migration | open |
| 2026-06-30 | `DEPT_OPTIONS` in `ApplyWizard.tsx` | option-list | Department interest checkboxes hardcoded | Source from `departments` table or `page_content` key | open |
| 2026-06-30 | `lib/send-email.ts` (sender = verified `glaum.ca`) | email | Sending domain/identity is Glåüm's | Per-community verified sender in `community_config`; see [`../`](./) email notes | open |
| 2026-06-30 | Supabase Storage buckets (`group-badges`, avatars, etc.) | storage | Upload paths are global, not namespaced per community | Prefix paths with `community_id/` | open |
| 2026-06-30 | Live domain `camp.glaum.ca`; no tenant resolution | domain/routing | Single-tenant routing; one origin | Decide subdomain-per-community vs custom domains; affects Clerk allowed origins, cookies, storage | open |
| 2026-06-30 | All entity tables lack `community_id` | query-scope/schema | Every table assumes one community | Add `communities` table + `community_id` FK + scope every query (foundation phase) | open |
| 2026-06-30 | `lead_up_events` (new) — see [`lead-up-gatherings.md`](./lead-up-gatherings.md) | schema | Assumes a single upcoming event + its runway; "lead-up vs at-camp" hardcodes one event's timeline | Generalize to *Event* as a first-class object: community → many events, each with its own lead-up phase + schedule | open |
| 2026-06-30 | `sendLeadUpGatheringEmail` in `lib/send-email.ts` | terminology/copy | Email copy says "on the way to camp" — "camp" is What-If/Glåüm-specific | Use a per-community event/term token in email copy | open |
| 2026-06-30 | `lib/asset-library.ts` `BUILTIN_ASSETS` (Glåüm Elder) | storage/branding | Community-specific medal art shipped as a code built-in (meant to be the shared global library) | Built-ins should hold only generic/global art; per-community art → tenant-scoped `asset_library` table (`community_id`) merged into the same picker; migrate Elder to a Glåüm upload | open |
| 2026-06-30 | `group_collections` (migration 042) — collection layer above `groups` | schema | Generalizes the flat Glåüm groups list (Setup/Decor/Teardown) into named, configurable collections ("Contributions", "Volunteer Teams", "Committees", "Guilds"). Platform no longer hardcodes group semantics. | This *is* the generalization; collections + selection mode are config-first. Only the backfill **seed name** `'Contributions'` is Glåüm framing — organizer renames it. | good-pattern |
| 2026-07-01 | `app/signup/page.tsx` — "Your Groups" section heading + description (also `GroupCommitments`) | terminology/copy | Was "Your Contributions" / "setup, teardown, decor" crews. Reworded to collection-neutral copy ("Your Groups" + generic opt-in line) that reads correctly for any community. | **Decided (Chante): leave hardcoded, do NOT make configurable.** Generic default is sufficient; per-section copy config would add admin-config sprawl for little value. Not a per-tenant blocker. | good-pattern |
| 2026-07-01 | `app/profile/SignupSection.tsx` `CALENDAR_DAYS` / `DAY_TO_DATE_LABEL` | schema/option-list | Member shift picker's day columns are hardcoded to Thu–Sun **July 23–26 2026** — any shift on another date silently vanishes | Shifts carry **real dates**; picker columns derive from the event's actual date range. See [`shifts-redesign.md`](./shifts-redesign.md) | **resolved 2026-07-01** — grid retired; picker groups by shift type with real `event_date` labels (also removed the second hardcoded July map, `CommitmentsSection` `DAY_LABELS`) |
| 2026-07-01 | Shift = `schedule_events` with a `capacity`; `contribution_type` = Setup/Teardown/Decor | schema/option-list | "Shift" isn't a first-class concept and contribution kinds are a hardcoded enum baked into the program table | Model shift **categories** as data (bound-to-group or required-of-all); requirement lives on the category. See [`shifts-redesign.md`](./shifts-redesign.md) | open |
| 2026-07-01 | `camp_signups` single `schedule_event_id`; attunement `shift` requirement = one global boolean | schema | Assumes a member works exactly **one** shift; What If needs ≥1 per owed category (contribution + volunteer) | Member holds many shifts; `shift` requirement becomes "≥1 in every owed category". See [`shifts-redesign.md`](./shifts-redesign.md) | open |
| 2026-07-01 | Dead `shifts` table + `/api/admin/shifts` routes | schema | Orphaned old-shift-system remnant, unused since the 2026-06-23 cleanup | Remove in the redesign migration | open |
| 2026-07-01 | `components/ScheduleCalendarClient.tsx` `DAYS` (Wed Jul 22–Mon 27) + `app/profile/PersonalScheduleCalendar.tsx` `DAY_META` (Wed Jul 23–Mon 28) | schema/option-list | The two *display* calendars hardcode their day columns to What-If's July week — and disagree with each other by one day. Events outside the window don't render | Derive day columns from the events' real `event_date` range (signup picker already does this since the shifts redesign) | **resolved 2026-07-01** — both calendars now build columns from `lib/schedule-days.ts` (configured event range ∪ real event dates); event range is admin-configurable (Configure → Structure → Event Dates, `config_event_start/end_date`) |
| 2026-07-01 | `public/asset-library/icons/*.webp` + `groups.icon_image` uploads | asset | Icon library is baked-colour raster art (ChatGPT-generated gold-on-ink) — colours, lighting, and background assumptions are frozen into the pixels, so it can never retint for another community palette | Single-colour `currentColor` SVGs that inherit the community palette via CSS; accent colour becomes a config parameter. First pass lives in `design/icon-lab/` (generator + gallery) | open |
| 2026-07-01 | `app/admin/overview/page.tsx` hardcodes: `HOURS_PER_MEMBER = 3`, Setup/Teardown/Decor card names, `RIDESHARE_OPTIONS` list, legacy `setup_limitations`/`setup_preference` columns | option-list/schema | Admin Overview dashboard bakes in Glåüm group names, a flat 3-hour quota, and the rideshare option list; also still reads retired setup_* columns | Derive team cards from `groups`/collections; hours from shift-type required hours (see [`shifts-redesign.md`](./shifts-redesign.md)); rideshare options from the form config | partially resolved 2026-07-01 — group cards now derive from the `groups` table and retired `setup_*` reads are gone; **2026-07-03: `HOURS_PER_MEMBER` gone** — Shift Hours now computes real scheduled/filled hours from `shift_types` + shift events + signups (`lib/shift-hours-overview.ts`); `RIDESHARE_OPTIONS` still hardcoded |
| 2026-07-02 | Participation-lead copy: "Offer to lead ✦" / "✦ Lead" / "Led by …" (`SignupSection.tsx`, `LeadUpGatherings.tsx`, `MemberSignupCard.tsx`) | terminology/copy | "Lead" as the label for a member running a shift/gathering is hardcoded member-facing copy; another community might say Facilitator, Steward, Host | Label-only concern, not structural — the `role` column value stays `lead`; if ever needed, a single term in community config | open |
| 2026-07-02 | Sacred-symbol trinity (eye/triangle/hands) woven through UI ornament & copy | asset/brand | The Glåüm trinity and its silly-sacred voice are community-specific brand vocabulary — another community would carry different symbols and tone | Ornament enters the UI only through centralized, swappable components/assets and theme-layer copy defaults (philosophy = flagship theme, structure = agnostic); see `docs/design-philosophy.md` §5 | open |
| 2026-07-02 | `app/admin/ScheduleManager.tsx` `DAY_ORDER` (Wednesday-first weekday ranking) | option-list | Admin schedule's weekday fallback order is What-If's Wed-start week; only used to sort legacy **undated** rows among themselves (dated rows and day sections order by real dates via `lib/schedule-days.ts`) | Dies naturally once every event carries an `event_date` (the UI now surfaces undated rows in an "Undated" fix-me bucket); otherwise derive week start from the configured event range | open |
| 2026-07-02 | `app/admin/LeadUpCalendar.tsx` — Sunday-first week, hardcoded English month/weekday labels, 12-hour clock display throughout (`lib/shift-hours.ts` `formatClock`) | copy | The admin month calendar (and all time display) assumes a North-American locale: week starts Sunday, en-US month names, AM/PM clock | Locale/week-start from community config when multi-tenant; the calendar generalizes otherwise (nothing event-specific — the tinted range comes from configured Event Dates) | open |
| 2026-07-02 | `COMMITMENT_TEXT`/`COMMITMENT_COLORS` (`SignupSection.tsx` + `app/roles/page.tsx`) | option-list/terminology | Commitment-level colour scale keys off hardcoded English labels (`Low`…`High`) — the `roles.commitment` free-text values are a de-facto Glåüm enum, and the map is copy-pasted in two files (kept per Chante in picker/registry; removed from the profile only) | Centralize the scale in one lib module; if commitment levels ever need structure, make them a configurable option list per community | open |
| 2026-07-02 | Regal icon set in `public/asset-library/icons/` (14 sculpted-gold webps; `raised-hand.webp` = style anchor) | asset/brand | The icon library is baked-gold raster art in Glåüm's regal register — not retintable; another community would need its own set struck in its own material | Icons already load by path (e.g. `groups.icon_image`), so a per-community asset directory swaps the whole set; new subjects strike in minutes via gpt-image-1 edits anchored to that community's style-anchor file (see `docs/design-philosophy.md` §7.9) | open |
| 2026-07-02 | `resource_lists` (migration 051) has no event scoping | schema | Shared-resource lists implicitly belong to *the* event — a single-event/single-community assumption (deliberate; see `docs/shared-resources.md` → Non-goals). A multi-event tenant needs lists scoped or copyable per event | Add an event/edition FK when the multi-tenant foundation lands; the community-scoped shape migrates cleanly | open |
| 2026-07-02 | `/participate` "Bring Something" copy — "The camp runs on what we carry in together" (`app/participate/page.tsx`, `ResourceCommitments.tsx` empty-state) | terminology/copy | "Camp"-flavoured framing for the shared-resources section; another community might not be a camp | Same call as the "Your Groups" heading (Chante: leave hardcoded, generic default is fine); theme-layer copy default if ever needed | open |
| 2026-07-02 | Role/shift self-serve gated on approved **attendance**: `/participate` redirect + `requireApprovedCamper` in `app/api/signup/route.ts` & `app/api/shift-signups/route.ts` | auth/schema | "Part of the camp" conflates *community membership* with *event attendance*. Roles already model pre-event work (`roles.responsibilities_before`, `commitment_period`), yet only approved attendees can hold one — real communities have non-attending contributors (pre-event designers, remote organizers) | Split the two facts: roles gate on community membership, shifts/attendance on event participation (foundation phase). Interim: admit a non-attending contributor as a regular member — nothing in the schema requires attendance | open |
| 2026-07-02 | `volunteers` as a parallel person table (duplicates name/phone/photo/etc. from `members`) | schema | Two person species is a single-event shortcut; every member surface (designations, rosters, groups, Many Hands) is blind to volunteers, so extending them self-serve access means throwaway two-table union code | Fold volunteers into `members` with a participation axis (attending / supporting); the volunteer form becomes a lighter application path into the same table. **Decided (Chante 2026-07-02): no volunteer self-serve until then** — intents stay admin signals; volunteer checklist shows a "we'll reach out" state (`TaskStatus.tsx`) | open |
| 2026-07-02 | `memberDisplayNames` (`lib/member-names.ts`) + admin surfaces (`overview/page.tsx`, `role-requests`, shift rosters) resolve people via `members` only | query-scope | Anyone without a members row is nameless/invisible in rosters & queues while their `camp_signups`/`member_shift_signups` rows (keyed on `clerk_user_id`) still consume capacity — ghost occupancy if non-members ever write signups | Dies naturally with the volunteers→members fold; until then keep signup writes member-gated (the current guards do) | open |
| 2026-07-02 | `vercel.json` cron `0 16 * * *` (attunement nudges) | schema/option-list | The daily nudge hour is hardcoded to 9am **Pacific** — one community, one timezone. Multi-tenant needs per-community send times (and one cron fan-out, not one cron per tenant) | Single daily cron sweeps all communities; each community row carries a timezone/send-hour and the route filters "due now" | open |
| 2026-07-02 | `lib/camp-event.ts` `FALLBACK_EVENT_START = '2026-07-23'` | option-list | Countdown fallback bakes in What If's opening day (used only when `config_event_start_date` is unset — the configured value wins) | Already config-first via Event Dates (Configure → Structure); drop the fallback once config is guaranteed per community | open |
| 2026-07-02 | `sendAttunementNudgeEmail` copy (`lib/send-email.ts`) — "The triangle holds, the eye witnesses…" + "attunement" vocabulary | terminology/copy | The nudge email speaks in Glåüm's trinity voice and "attunement" is community vocabulary baked into subject + body + the prefs toggle label | Theme-layer copy defaults (same story as the trinity row above); "attunement" term becomes community config alongside the checklist itself | open |
| 2026-07-02 | Admin Program tab subtitle — "The schedule of camp and the gatherings on the runway to it" (`app/admin/program/page.tsx`); Members tab h1 "ManyHands Registry" unchanged | terminology/copy | "Camp" framing in admin chrome; the tab structure itself (Overview/Members/Program/Configure) is community-agnostic | Theme-layer copy default when multi-tenant; the IA generalizes as-is | open |
| 2026-07-03 | Home attunement banner hardcodes `/asset-library/icons/eye-in-triangle.webp` as its ornament (`app/page.tsx`) | terminology/copy | The eye-in-triangle is Glåüm trinity vocabulary (attunement = eye-territory); another community would carry a different symbol for its status surface | Ornament should resolve through the theme layer (same story as the trinity rows above) — asset key per community, not an inline path | open |
| 2026-07-03 | Avatar-dropdown menu item "About Glåüm" (`components/HeaderClient.tsx`) | terminology/copy | Community name baked into a nav label (her request — the bare "About" read ambiguous next to profile-ish items) | Label becomes `About {community.name}` from the tenant config | open |
| 2026-07-02 | Member-visible claimant names on the Bring Something board (`/api/resources` GET `claimants`) | policy/assumption | The preparation board shows every member who committed an item by name ("✓ Erik ✓ Sarah") — assumes a small high-trust community where social proof is welcome; a larger/privacy-sensitive community might want totals-only | Per-community toggle (names vs counts) in community config when multi-tenant lands; the API already computes both | open |
| 2026-07-03 | `lib/member-facts.ts` `ATTENDED_YEARS_KEY = 'gatheringsAttended'` | schema | `joined_year` ("Member since") derives from an **admin-created** profile field key that only exists in Glåüm's registry — another community without that field silently falls back to application year | Tenure source becomes a pointer in community config ("which multi-select field holds attendance years"), or a first-class built-in tenure field in the registry | open |
| 2026-07-03 | Apply-wizard track picker heading "What If 2026" + hardcoded `DEPT_OPTIONS` department list (`app/apply/TrackPicker.tsx`, `app/apply/ApplyWizard.tsx`) | terminology/copy | Event name + Glåüm's department names baked into the wizard (encountered during the pre-What-If QA sweep; the departments table exists but the wizard's checkbox list is a frozen copy) | Event name from community config; `dept_interests` options should read the live `departments` table | open |
| 2026-07-03 | Radio copy speaks in "camp" ("joined the camp.", "Day N of camp", "The pulse of camp", email "…what's happening around camp ✦") — `lib/radio.ts`, migration `061` backfill, `app/radio/*`, `/api/admin/radio` | terminology/copy | "Camp" framing baked into stored feed messages, the welcome line, and the broadcast-alert email; stored rows keep the wording forever | Message templates move to the theme layer; note stored `radio_events.message` rows are snapshots — a rebrand doesn't rewrite history (acceptable: the feed is a historical record) | open |
| 2026-07-03 | Radio "today" + camp-day math use server UTC dates (`lib/radio.ts` `isoToday`, welcome-line day number) | policy/assumption | Site-wide convention (`toISOString().slice(0,10)` everywhere): near-midnight members can see the wrong "today" for a community far from UTC; the Now/Up-next strip mitigates by picking now/next against the member's device clock | Per-community timezone in tenant config; all "today" derivations resolve through it | open |
| 2026-07-03 | Radio event icons default to specific emoji (✦ member, ✨ resource, 🏅 distinction, 📢 broadcast) in the source hooks | terminology/copy | Iconography defaults chosen for Glåüm's voice; also stored per-row so they persist | Default icon set per kind moves to community config; rows keep their snapshot | open |
| 2026-07-03 | Radio editorial copy builders (`lib/radio.ts`): welcome detail "Say hello if you see them around camp.", milestone detail "many hands make light work ✦", welcome headline uses `SITE_NAME` | terminology/copy | The feed's voice is Glåüm's voice — warm camp-register copy baked into the builders (better than before: all copy now lives in ONE place, and the welcome headline already resolves through `SITE_NAME`) | Post builders become per-community template strings in the theme layer; `radio_events` rows are historical snapshots either way | open |
| 2026-07-03 | Admin Community tab h1 "Community" + subtitle "The people of camp, what they hear, and what they're bringing" (`app/admin/page.tsx`) — renamed from "Members" / h1 "ManyHands Registry" when Shared Resources moved in | terminology/copy | The Glåüm-specific "ManyHands" term left the admin chrome (narrows the 2026-07-02 admin-chrome row); "camp" framing remains in the subtitle; tab structure (Overview/Community/Program/Configure) stays community-agnostic | Subtitle copy from the theme layer when multi-tenant; the IA generalizes as-is | open |
| 2026-07-03 | Member suspension copy — self-serve panels + banners speak in warm register ("Life happens.", "Welcome back!", "Attendance paused"); admin note "…won't be counted in the participation numbers" (`app/profile/ProfileSettings.tsx`, `app/admin/SuspendMemberButton.tsx`, `app/page.tsx`, `app/profile/page.tsx`) — migration `063` | terminology/copy | Suspension is a generic community-membership concept, but the microcopy is Glåüm's voice; nothing else here is single-community (the mechanism is `members.suspended_at`, tenant-agnostic) | Copy moves to the theme layer with the rest of the member-facing strings; the suspension mechanism generalizes as-is | open |
| 2026-07-06 | Shift **roster is visible to all approved members** — the participate picker shows who's signed up on each shift (initial-disc cluster on the card + a named "Signed up (N)" list in the confirm modal), disclosing every holder's full display name member-to-member (`lib/participate-data.ts` `roster`, `app/profile/SignupSection.tsx`) | policy/assumption | Consistent with existing disclosure (members already see each other on `/members`; lead names already showed on these cards) — a fine default for a small tight-knit camp, but hardcoded "everyone sees everyone" with no privacy control | Multi-tenant: a per-community visibility policy for signup rosters (all members / leads+admins only / count-only), resolved in `getShiftSignupData` | open |
| 2026-07-08 | Shared Resources is **wiki-open**: any approved member creates lists and edits/deletes any list's items; only list *deletion* is admin-gated (`app/api/resources/lists,items/**`, `ResourceCommitments.tsx` `canManage`/`canDeleteLists`) | policy/assumption | Removes the old single-community "admin authors the needs" assumption, but replaces it with another: "everyone can edit everyone's lists" suits a small high-trust camp; a larger community might want per-creator edit rights, moderation, or an approval step. Lists carry **no owner column** by design | Multi-tenant: a per-community resource-authoring policy (open / creator-owned / admin-curated), and add `resource_lists.created_by` if per-owner rights are ever needed (migrates cleanly) | open |
