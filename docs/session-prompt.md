# Session brief — moved to the repo root

The session brief is now **`CLAUDE.md` at the repo root** (moved 2026-08-27),
loaded automatically at the start of every Claude session — nothing to paste
anymore. Same pattern as the All Hands repo.
